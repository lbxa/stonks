[project]
name = "core"
version = "0.1.0"
description = "Shared equity analysis primitives and utilities."
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
]

[build-system]
requires = ["uv_build>=0.8.10,<0.9.0"]
build-backend = "uv_build"
